type Alert = {
  id: number;
  image: string;
  title: string;
  address: string;
  dateTime: string; // change to date/time later on when api is redy
  caption: string;
  opCenter: string;
  opCenterImg: string;
};

export default Alert;
